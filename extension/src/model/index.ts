import wasm from '../main.go'
// const wasm = require("../main.go")

const { MyGoFunc } = wasm;


export function runModel() {
  // let result = await add<number, string>("12", "13");
  // this.setState({ value: newValues, result });
}
